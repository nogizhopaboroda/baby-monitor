#!/bin/sh

ecasound -a:all -f:16,1,44100 -i $AUDIODEV -b:$AUDIO_BUFFER_SIZE \
	 -a:1 -f:$RAW_AUDIO_FORMAT,$AUDIO_CHANNELS,$AUDIO_SAMPLE_RATE -eadb:$AUDIO_GAIN -o $PIPES_DIR/$RAW_AUDIO_RECORDING_PIPE
	 -a:2 -eadb:$AUDIO_GAIN -o $PIPES_DIR/$AUDIO_RECORDING_PIPE
